# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Virtual environments
.venv/
venv/
env/

# Tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Local secrets / config (never commit real ones)
.env

# Model / HF caches (must NOT be committed — pulled at build time)
.cache/
.fastembed_cache/
*.onnx
models/

# Derived retrieval index (baked at build time / regenerated by `make ingest`)
data/index.npz

# OS
.DS_Store
Thumbs.db
