# Runtime dependencies (pinned for reproducibility).
# Note: fastembed pulls onnxruntime + tokenizers; it deliberately avoids the
# heavy torch/CUDA tree because we run the embedding model on CPU only.
fastapi==0.115.6
uvicorn[standard]==0.49.0
pydantic==2.10.4
pydantic-settings==2.7.1
httpx==0.28.1
fastembed==0.8.0
numpy==2.2.1
structlog==24.4.0
PyYAML==6.0.2
SQLAlchemy==2.0.36
Jinja2==3.1.4
python-multipart==0.0.20
